# Homework 6: Swapping Two Numbers 🔄

## 📝 Problem Statement
Write a program that reads two variables, `num1` and `num2`. 
Your target is to **swap** (exchange) their values.

**Example:**
If the user inputs `7` and `231` (so `num1 = 7` and `num2 = 231`), after swapping, `num1` should become `231` and `num2` should become `7`.

## 💻 Starter Code

```cpp
#include<iostream>
using namespace std;

int main() {
    int num1, num2, num3 = -1;
    
    cin >> num1 >> num2; // Let's say we read 7 and 231

    // TODO: Write 3 lines of code that swap them
    // 1.
    // 2.
    // 3.

    cout << num1 << " " << num2 << endl; // This should print 231 7
    
    return 0;
}